# GitLab CI: 自动同步到 GitHub
sync_to_github:
  image: alpine:latest
  only:
    - master
  before_script:
    - apk add --no-cache git curl
    - git config --global user.email "bot@zstack.io"
    - git config --global user.name "GitLab Bot"
    - git clone --bare . /repo.git
  script:
    - >
      cd /repo.git &&
      git push --mirror https://HeyMax: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@github.com/HeyMax/zstack-ai-assistant.git
  when: always
